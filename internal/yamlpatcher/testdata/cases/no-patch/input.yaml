apiVersion: v1
kind: Service
metadata:
  annotations:
    postrenderer.helm.sh/postrender-filename: foo-patch
  name: my-service
spec:
  selector:
    app.kubernetes.io/name: MyApp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
